# Terraform state files (local backend)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude override files as they are usually used for local overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform lock file (commit this for consistent provider versions)
# .terraform.lock.hcl

# Terraform working directory
.terraform/

# Sensitive variable files (if any)
*.auto.tfvars
secret.tfvars

# IDE
.idea/
*.swp
*.swo
