{
  "name": "r2-notification-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "echo 'DEPRECATED: Worker is now deployed via Terraform. Use: cd infra/terraform/environments/dev && infisical run --env=dev --path=\"/terraform\" -- tofu apply'",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.59.3"
  }
}
