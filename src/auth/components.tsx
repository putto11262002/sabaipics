/**
 * Auth Components - Re-exports from Clerk
 *
 * These are simple re-exports for now (YAGNI principle).
 * We can add wrappers later when we need customization:
 * - Custom styling
 * - Analytics tracking
 * - Error handling
 * - Accessibility improvements
 */

export { SignIn, SignUp, UserButton, SignedIn, SignedOut } from '@clerk/clerk-react';
