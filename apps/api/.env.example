# Cloudflare Workers Environment Variables
# Copy to .env.development, .env.staging, or .env.production

# Clerk Authentication (get from Clerk Dashboard → API Keys)
CLERK_SECRET_KEY=sk_test_xxxxx
CLERK_PUBLISHABLE_KEY=pk_test_xxxxx

# Clerk JWT Public Key for networkless verification
# Format: Single line with \n escape characters
# Get from: Clerk Dashboard → API Keys → Show JWT public key
CLERK_JWT_KEY="-----BEGIN PUBLIC KEY-----\nMIIBIjANBg...\n-----END PUBLIC KEY-----"

# Clerk Webhook Signing Secret
# Get from: Clerk Dashboard → Webhooks → Add Endpoint → Copy Signing Secret
CLERK_WEBHOOK_SIGNING_SECRET=whsec_xxxxx

# Database Connection
# For production: Use wrangler secret put DATABASE_URL
# For development: Your Neon database connection string
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ngrok Domain (development only)
# Get from: https://dashboard.ngrok.com/domains → New Domain
NGROK_DOMAIN=your-name.ngrok-free.dev

# Note: Non-secret variables (CORS_ORIGIN, AUTHORIZED_PARTIES, AWS_REGION)
# are configured in wrangler.jsonc under each environment's vars section.
