# SabaiFace Logs

The repository keeps project logs in the top-level `log/` directory (repo convention).

Recognition tuning notes:

- `log/012-recognition-tuning-summary.md`
- `log/013-recognition-grid-search-results.md`
