# Tech Playbooks (Deprecated)

This folder is kept for backwards compatibility only.

Canonical location:
- `.claude/playbook/**`

See authoring rules and folder conventions:
- `.claude/playbook/README.md`

## Relationship to other tiers

- Tech Image (`.claude/tech-image/**`) = high-level “what + governance”.
- Playbooks (`.claude/playbook/**`) = project-independent “how” by topic/tech.
- Project Bindings (`.claude/rules/**`) = repo-specific wiring (paths, chosen libs, contracts).
