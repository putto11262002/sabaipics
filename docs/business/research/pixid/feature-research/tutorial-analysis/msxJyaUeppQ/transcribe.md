# คู่มือปลั๊กอิน Pixid Lightroom

**Video ID**: msxJyaUeppQ

## Transcript

**[00:00]** สวัสดีครับวันนี้จะมาสอนวิธีการใช้งาน pixis Lightroom plugin นะครับซึ่งจะเป็นตัวเวอร์ชั่นใหม่ด้วยแล้วก็เราจะสอนการใช้ plugin Lightroom ทั้ง 2 แบบนะครับแบบต่อกับพิกเลยกับแบบ ftp นะครับผมเริ่มแรกนะครับให้มาที่ตัว pixis / นะครับแล้วก็จะมีลิงก์ดาวน์โหลดตามนี้นะ

**[00:28]** ครับเราก็กดดาวน์โหลดลิงก์ได้เลยนะครับไฟล์ที่ได้เนี่ยจะเป็นไฟล์ zip นะครับผมผมก็จะเลือกไปวางไว้ที่เอ่อสมมุติว่าโฟลเดอร์นี้นะแล้วก็โอเคผมจะทำการ unzip ออกนะครับก็จะได้เป็นโฟลเดอร์นี้นะครับอ่าโฟลเดอร์นี้นะครับคราวนี้วิธีการใส่ plugin นะครับก็ให้ไปที่ Lightroom

**[01:03]** นะครับไปที่เมนูไฟล์แล้วก็ plugin Manager นะครับแล้วก็ให้กด Add นะครับผมคราวนี้ก็ไปที่โฟลเดอร์ที่เรา unzip file ไว้เมื่อกี้นะครับผมอ่าแล้วก็คลิกเข้าไปจนไม่มีอะไรให้คลิกแล้วนะครับแล้วก็กด select Folder โอเคมันก็จะขึ้นมาแบบนี้นะครับแล้วก็กดดันเรียบร้อยนะ

**[01:32]** ครับก่อนการใช้งานนะครับอ่าเราต้องทำการ Tester ก่อนนะครับก็คือต่อกล้องเข้าไ room นะครับผมก็จะใช้สายวันนี้ผมใช้สายของ oric นะครับก็ต่อกล้อง C6 นะครับเสร็จแล้วให้ไปที่เมนูไฟล์นะครับ Tester Capture นะครับแล้วก็ start Tester นะครับตัวนี้ก็จะเป็น SE name

**[02:02]** นะครับอันนี้เราต้องตั้งชื่อแล้วก็จำชื่อไว้นะครับสมมุติว่าผมใส่เป็น pix Test นะครับกดโอเคตัว Lightroom ก็จะสร้าง Folder ตัวนี้ขึ้นมานะครับชื่อ pixis Test ซึ่งตรงเนี้ยเอ่อบางคนอาจจะมีปัญหาคือมันไม่ขึ้นเพราะว่าอาจจะยังไม่ได้สร้างแคตาลอกนะครับตรงนี้ก็ต้องไปสร้าง

**[04:12]** ครับเราก็มาที่เมนู plugin Extract นะครับหลังจากเราลงได้แล้วเนี่ยมันจะขึ้นตัวนี้มาให้นะครับก็คือ Export uploader Console นะครับโอเคหน้าตาก็จะเป็นอย่างนี้นะครับ panel เอ่อช่องแรกนะครับตัวนี้ที่มันเขียนว่า select Light room Folder นะครับคือจะเป็นโฟลเดอร์ที่เรามันคือ SE

**[04:39]** Name ที่เราสร้างไว้เมื่อกี้นั่นแหละครับโอเคเราก็เลือกตามนี้เลย pix Test นะครับตัวนี้นะครับโอเคขนาดรูปไซสตั้งได้นะครับ 2,000 3,000 4,000 5,000 6,000 รวมถึง Original นะครับเดี๋ยวผมจะลองตั้งไซส์ซักเอ่อ 5,000 แล้วกันนะครับแล้วตัว output Folder output

**[07:16]** นะครับโอเคเนี่ยครับเราก็จะได้รูปมาแล้วนะครับแล้วก็คราวนี้เราจะอัพรูปขึ้นพิกยังไงนะครับก็มาที่หน้านี้นะครับพิกดาวน์โหลดนะครับตัวนี้จะเป็นตัว uploader นะครับครับก็มี 3 เวอร์ชั่นมี Windows Mac จะมี 2 อันนะครับคือเป็น Mac silicon ตัวนี้ก็คือเป็นพวก M1 M2 M3

**[11:30]** ถ้าเราไม่ใช้งานนะครับก็ให้กด Stop หยุดได้เลยนะครับไม่งั้นมันจะรันต่อไปเรื่อยๆนะครับโอเคอันนี้ก็คือเป็นวิธีแรกนะครับที่ใช้ตัว Plug in Light room แล้วก็ต่อกับ pixis uploader นะครับคราวนี้จะไปวิธีที่ 2 ก็คือเป็น plugin Light room ต่อ fdp โดยตรงนะครับผมจะปิดตัว pixis เลยนะ

**[14:44]** ประมาณนี้โอเคก็จบการใช้งาน Plug อินลุนะครับ

---

**Video URL**: https://www.youtube.com/watch?v=msxJyaUeppQ
**Extraction Date**: 2025-12-01T02:45:00.000Z
**Source**: YouTube-transcript.io automated extraction

### Key Technical Features:
- Lightroom plugin integration (version 0.4.0)
- Dual upload methods: Desktop app + Direct FTP
- Multiple output sizes (2000-6000px + original)
- File size limits and compression options
- Batch processing with folder monitoring
- Cross-platform support (Windows, Mac Intel, Mac Silicon)
- Preset application during export
- Real-time tethered shooting support
